# 改行コードの統一。OS をまたいでも同じバイト列になるようにする。
* text=lf

# 言語統計の制御。docs/ は文書であってコードではない。
docs/** linguist-documentation=true

# 生成物。terraform-docs が書き出す区間を持つファイルは、差分レビューの対象にしない
# （ADR-0701 決定4）。生成器を配線した時点でここへ加える。
