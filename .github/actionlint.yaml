# actionlint configuration
# https://github.com/rhysd/actionlint/blob/main/docs/config.md
#
# Empty today: this repository uses neither a self-hosted runner nor `vars.*`. The two blocks
# below are the shapes to fill in if either arrives.

# self-hosted runner labels (suppresses the "unknown runner label" false positive)
# self-hosted-runner:
#   labels:
#     - my-custom-runner

# Declared `vars.*` (lets actionlint flag a reference to an undeclared one)
# config-variables:
#   - DEPLOY_ENV
